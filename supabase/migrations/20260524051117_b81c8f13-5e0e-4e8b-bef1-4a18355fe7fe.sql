UPDATE public.posts SET excerpt = 'Inside a 90-day teardown: seven health signals, three thresholds, one playbook per threshold, and the operating cadence that made prediction the default.' WHERE slug = 'chargebee-reactive-to-predictive';

UPDATE public.posts SET excerpt = 'Sub-100% is a product problem dressed as a CS problem. Here is the account-scoring, trigger library, and Sales-CS hand-off the top quartile actually runs.' WHERE slug = 'nrr-120-expansion-playbook';

UPDATE public.posts SET excerpt = 'PLG telemetry feeding workspace-health scores, pod-based coverage, and expansion plays that fire on usage signals rather than calendar reminders.' WHERE slug = 'notion-105-to-128-nrr';